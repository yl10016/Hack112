#To install CMU Graphics

1. To install a desktop version, download the folder 'cmu_graphics' to the same directory as the app
2. To install CMU Graphics on VSCode, run the python file 'install-cmu-graphics.py'